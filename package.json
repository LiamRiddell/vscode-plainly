{
  "name": "contenter",
  "displayName": "Contenter",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.61.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:contenter.contentfyLint"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "contenter.contentfyLint",
        "title": "Contenter: Check Document"
      }
    ],
    "configuration": {
      "title": "Contenter",
      "properties": {
        "contenter.wordLists.problematic": {
          "type": "string",
          "description": "This is the list of problematic words.",
          "editPresentation": "multilineText",
          "default": ""
        },
        "contenter.wordLists.problematicCustom": {
          "type": "array",
          "markdownDescription": "A list of relative file paths pointing to JSON files following the [custom data format](https://github.com/microsoft/vscode-css-languageservice/blob/master/docs/customData.md).\n\nVS Code loads custom data on startup to enhance its CSS support for the custom CSS properties, at directives, pseudo classes and pseudo elements you specify in the JSON files.\n\nThe file paths are relative to workspace and only workspace folder settings are considered.",
          "default": [],
          "items": {
            "type": "string",
            "description": "Relative path to a custom word list file"
          }
        }
      }
    }
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "yarn run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.1.4",
    "@types/mocha": "^9.0.0",
    "@types/node": "14.x",
    "@types/vscode": "^1.61.0",
    "@vscode/test-electron": "^1.6.2",
    "eslint": "^7.32.0",
    "glob": "^7.1.7",
    "mocha": "^9.1.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "moo": "^0.5.1"
  }
}
