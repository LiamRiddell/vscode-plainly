{
  "name": "contenter",
  "displayName": "Contenter",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.61.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:contenter.lint",
    "onCommand:contenter.clear"
  ],
  "main": "./contenter/extension.js",
  "contributes": {
    "configuration": {
      "title": "Contenter",
      "properties": {
        "contenter.experimental.search.advancedSearch": {
          "type": "boolean",
          "scope": "resource",
          "default": true,
          "description": "Enable the advanced search, which tries handle code snippets and basic search bypass techniques."
        },
        "contenter.enableWordLists.problematic": {
          "type": "boolean",
          "scope": "resource",
          "default": true,
          "description": "Enable the problematic wordlist when linting documents."
        }
      }
    },
    "menus": {
      "editor/title": [
        {
          "when": "resourceLangId == markdown",
          "command": "contenter.lint",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "when": "resourceLangId == markdown",
          "command": "contenter.clear",
          "group": "navigation"
        },
        {
          "when": "resourceLangId == markdown",
          "command": "contenter.lint",
          "group": "navigation"
        }
      ]
    },
    "commands": [
      {
        "command": "contenter.lint",
        "title": "Contenter: Check Document"
      },
      {
        "command": "contenter.clear",
        "title": "Contenter: Clear Document"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "yarn run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.1.4",
    "@types/mocha": "^9.0.0",
    "@types/node": "14.x",
    "@types/vscode": "^1.61.0",
    "@vscode/test-electron": "^1.6.2",
    "eslint": "^7.32.0",
    "glob": "^7.1.7",
    "mocha": "^9.1.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "moo": "^0.5.1"
  }
}
